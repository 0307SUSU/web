<header style="width:100%;height:60px;display:flex;justify-content:center;font-size:42px;line-height:60px;color:#11dd99">前端面试题总结</header>

## <font style="font-size:24px;font-weight:600;">CSS问题</font>

### <font style="font-size:18px;font-weight:600;">介绍一下标准的CSS盒子模型</font>

> 准的CSS盒子模型包括内容content，边框border，内边距padding，外边距margin

### <font style="font-size:18px;font-weight:600;">CSS选择器有哪些？哪些属性可以继承？选择器的优先级</font>

> 选择器：标签选择器、*通配符选择器、id选择器、class类选择器、子选择器>、相邻元素选择器+、后代选择器、属性选择器、伪类选择器
>
> 可继承的属性：字体属性font，颜色color
>
> 优先级：!important优先级最高，其次是id>class>标签...
>
> 优先级的计算方法：按顺序!important最高，如果出现相同优先级则选择最后出现的样式，继承的样式优先级最低

### <font style="font-size:18px;font-weight:600;">**如何居中div？如何居中一个浮动元素？如何让绝对定位的div居中？**</font>

> 如果普通div通过设置margin：0 auto；
>
> 如果是浮动元素float可通过position定位实现，设置position：absolute;left:50%;top:50%;设置margin上边距为元素高度的一半，左边距为元素宽度的一半
>
> 如果是绝对定位的元素可以设置left0；right0；margin0 auto，
>
> 还可以通过flexbox布局实现元素的居中，设置父元素display：flex布局，设置justify-content：center;aline-items:center;

## <font style="font-size:24px;font-weight:600;">JavaScript&TypeScript问题</font>

#### <font style="font-size:18px;font-weight:600;">事件冒泡与事件捕获原理</font>

事件冒泡（Event Bubbling）和事件捕获（Event Capturing）是描述事件传播方式的两种机制。

事件捕获从最外层祖先元素向目标元素传播，

而事件冒泡则相反，从目标元素向最外层祖先元素传播。

在实际开发中，我们更多地使用事件冒泡来处理事件，因为这样可以更方便地实现事件委托和更好地控制事件流程。

使用 e.stopPropagation()来阻止事件冒泡。作用当然是阻止我们触发它上级的事件啦

#### <font style="font-size:18px;font-weight:600;">call、apply、bind的区别</font>

共同点：

- 都是用来改变函数的this对象的指向的
- 第一个参数都是this要指向的对象
- 都可以参加后续参数传参

不同点：

- bind是返回对应函数，便于稍后调用；apply、call则是立即调用
- apply和call功能一样，只是传入的参数列表形式不同，call 需要把参数按顺序传递进去，而 apply 则是把参数放在数组、伪数组里。

#### <font style="font-size:18px;font-weight:600;">闭包</font>

一个作用域可以访问另外一个函数内部的局部变量，或者说一个函数（子函数）访问另一个函数（父函数）中的变量。此时就有闭包产生，那么这个变量所在的函数我们就称之为闭包函数。

优缺点：闭包的主要作用是延伸了变量的作用范围，因为闭包函数中的局部变量不会等着闭包函数执行完成就销毁，因为还有别的函数需要调用它，只有等这所有的函数都调用完了它才会被销毁。

如何解决：用完之后手动释放。

#### <font style="font-size:18px;font-weight:600;">内存泄漏</font>

内存泄漏指的是我们用动态存储分配的函数来开辟空间，但是在使用完了没有释放，结果就一直占据该内存单元，直到程序结束。简而言之就是用完了还没回收这就是内存泄漏。

1. **意外的全局变量**：在全局作用域中声明的变量如果没有及时释放，会导致对象一直存在于内存中，无法被垃圾回收。
2. **循环引用**：两个对象互相引用，且都不再被使用，但由于彼此之间仍然存在引用关系，导致无法被垃圾回收。
3. **定时器和事件监听器未清理**：定时器和事件监听器如果没有被及时清理，会导致对象一直存在于内存中，无法被释放。
4. **DOM 引用未清理**：如果某个元素被从 DOM 树中移除，但仍然被引用着，会导致该元素无法被垃圾回收。
5. **闭包**：闭包中的函数引用了外部函数的变量，导致外部函数的变量无法被释放，从而造成内存泄漏。

为了避免内存泄漏，可以采取以下几个方法：

- 避免创建不必要的全局变量。
- 注意及时清理不再使用的对象引用。
- 避免循环引用。
- 在使用定时器和事件监听器时，确保在不需要时及时清理。
- 在使用闭包时，注意内存占用情况，避免不必要的引用。

#### **说说从输入 URL 到看到页面发生的全过程**

当用户在浏览器中输入 URL 后，浏览器首先进行 DNS 解析，将域名解析成 IP 地址，

然后通过 TCP 协议与服务器建立连接。

接着，浏览器向服务器发起 HTTP 请求，服务器接收到请求后处理并返回响应。

浏览器接收到响应后，开始解析 HTML 和 CSS，构建 DOM 树和渲染树，最终将页面内容渲染到用户的屏幕上。

整个过程包括 DNS 解析、建立 TCP 连接、发起 HTTP 请求、服务器处理响应、浏览器解析响应、构建 DOM 树和渲染页面等步骤。

#### https与http

> HTTPS（Hyper Text Transfer Protocol Secure）和 HTTP（Hyper Text Transfer Protocol）之间的主要区别在于安全性。HTTPS 使用 SSL/TLS 协议对传输的数据进行加密，确保数据在传输过程中不被窃取或篡改，同时也提供了服务器身份验证和数据完整性保护。相比之下，HTTP 不对数据进行加密，容易被窃取和篡改，也没有身份验证机制，存在被中间人攻击的风险。因此，在需要保护数据安全和隐私的情况下，推荐使用 HTTPS。

#### **说说如何解决跨域问题?**

- 首先了解下浏览器的同源策略 同源策略 / SOP （ Same origin policy ） 是一种约定，由Netscape 公司 1995 年引入浏览器，它是浏览器最核心也最基本的安全功能，如果缺少了同源策略，浏览器很容易受到 XSS 、 CSFR 等攻击。所谓同源是指"协议+域名+端口"三者相同，即便两个不同的域名指向同一个 ip 地址，也非同源。
- 通过 jsonp 跨域
- document.domain + iframe 跨域
- 前端webpack设置webpack-dev-server，nginx 代理跨域
- nodejs 中间件代理跨域
- 后端在头部信息里面设置安全域名解决跨域

#### 浏览器缓存

> 强缓存、协商缓存，
>
> 强缓存通过设置响应头实现，可以直接查询加载资源，无需向服务器发送请求
>
> 协商缓存通过设置请求头和响应头实现，至少需要向服务器发送一次请求，服务器根据请求判断并返回资源
>
> 一般情况下浏览器会先查询是否存在强缓存，如果存在直接从本地缓存中加载资源，如果不存在则向服务器发送请求验证协商缓存
>
> 1. 常用的请求头有：
>    - `If-Modified-Since`：表示资源的最后修改时间，服务器会根据这个时间判断资源是否有更新。
>    - `If-None-Match`：表示资源的唯一标识符（通常是一个 hash 值），服务器会根据这个标识符判断资源是否有更新。
>
> 协商缓存的响应头有：
>
> - `Last-Modified`：表示资源的最后修改时间。
> - `ETag`：表示资源的唯一标识符。
>
> 常见的强缓存响应头
>
> - `Expires`：指定资源的过期时间，是一个绝对时间，即到了指定时间后资源过期。
>
> - ```
>   Cache-Control
>   ```
>
>   ：用于控制缓存的行为，常见的值有：
>
>   - `public`：表示响应可以被任何中间缓存（如CDN）缓存。
>   - `private`：表示响应只能被浏览器缓存，中间缓存不能缓存。
>   - `max-age=<seconds>`：指定资源在本地缓存中的最大存储时间，单位为秒。

#### 浏览器存储数据方式

SessionStorage、LocalStorage 和 Cookie 都是浏览器端存储数据的方式，它们有一些区别：

- `SessionStorage` 数据存储在浏览器的会话中，会话结束时数据被清除；`LocalStorage` 数据永久存储在浏览器中，除非手动清除；`Cookie` 可以设置过期时间，也可以在同一站点的不同页面之间共享。
- `SessionStorage` 和 `LocalStorage` 通常支持的容量比 `Cookie` 大得多，一般为 5MB 左右；`Cookie` 的容量限制为 4KB。
- `Cookie` 会在每次 HTTP 请求时发送到服务器，可以用于与服务器通信；`SessionStorage` 和 `LocalStorage` 仅在客户端存储，不会自动发送到服务器。
- `SessionStorage`、`LocalStorage` 和 `Cookie` 的 API 使用方式有所不同，但都提供了类似的操作方法。
- `Cookie` 可以设置安全标志和 HttpOnly 标志，提高安全性；`SessionStorage` 和 `LocalStorage` 存储的数据相对不太安全，容易受到 XSS 攻击。

#### **说说 ajax、fetch、axios 之间的区别**

> Ajax 是一种使用 JavaScript 发起异步 HTTP 请求的技术，可以使用原生的 XMLHttpRequest 对象或者各种库来实现。
>
> Fetch 是一种现代的 JavaScript API，用于发起网络请求，基于 Promise 实现，更现代化和方便。
>
> Axios 是一个基于 Promise 的 HTTP 客户端，提供了更多功能和友好的 API，可以用在浏览器和 Node.js 环境中。
>
> Fetch 是浏览器原生提供的 API，Axios 则是一个功能丰富的第三方库。

#### **JavaScript 深浅拷贝?**

- **浅拷贝**
- Object.assign
- 或者展开运算符
- **深拷贝**
- 可以通过 JSON.parse(JSON.stringify(object)) 来解决

浅拷贝只复制对象或数组的第一层属性或元素，而深拷贝会递归复制所有层级的属性或元素。

常见的浅拷贝方法包括 Object.assign()、Array.prototype.slice()、扩展运算符 ...，

而实现深拷贝可以使用递归、JSON.parse(JSON.stringify(obj))、第三方库等。

需要注意的是，深拷贝可能会导致循环引用的问题，需要特殊处理。

#### Promise（承诺）

*Promise 是ES6引入的异步编程的新解决方案。比传统的解决方案（回调函数）更加合理和更加强大*

- **状态**：pending（进行时）、fulfilled（已成功）、rejected（已失败）
- **用法：** Promise 对象是一个构造函数，会接受一个函数作为参数，这个函数的两个参数分别是 `resolve`和`reject`
  - `resolve` 函数的作用是将 **Promise** 对象的状态从 “未完成” 变为 “成功”
  - `reject` 函数的作用是将 **Promise** 对象的状态从 “未完成” 变为 “失败”

```
const promise = new Promise(function(resolve, reject) {});
```

- **实例方法：**

  - then() ---- 实例状态发生变化时，触发的回调函数，第一个参数是 `resolved`状态的回调函数，第二个参数是 `rejected` 的回调函数（一般使用 catch 方法来替代第二个参数）
  - catch() ---- 用于指定发生错误的回调函数
  - finally() --- 用于指定 不管 Promise 对象最后状态如何，都会执行的操作

  链式调用

  Promise 的链式调用是指在一个 Promise 的回调函数中返回一个新的 Promise，从而实现对多个异步操作的依次执行和处理。

#### 前端大文件下载暂停如何实现

1. **使用`XMLHttpRequest`或`fetch` API进行文件下载**：首先，使用`XMLHttpRequest`或`fetch` API从服务器下载大文件。这将返回一个可读取的文件流。
2. **分段下载文件**：为了实现暂停功能，可以将文件分成多个部分进行下载。每次下载一个部分，然后将这些部分拼接起来。
3. **暂停下载**：在下载过程中，可以通过中断下载请求（`XMLHttpRequest.abort()`或`fetch` API的`AbortController`）来暂停下载。记录已下载的部分内容和当前下载进度。
4. **继续下载**：在暂停后，用户要求继续下载时，可以根据已下载的部分内容和当前下载进度，发送新的下载请求，继续下载剩余部分的文件。

## <font style="font-size:24px;font-weight:600;">vue2&vue3问题</font>

#### vue中的Data为什么不是对象而是一个函数？

> 

#### vue双向绑定原理

> Vue 2 的双向绑定原理：
>
> 1. **数据劫持**：Vue 在初始化时会对 data 中的每个属性使用 `Object.defineProperty` 方法进行劫持，将属性改为 getter 和 setter，从而实现对属性的监控。
> 2. **模板编译**：Vue 的模板编译器会解析模板中的指令和插值表达式，生成一个 render 函数。在 render 函数中，会通过访问 data 中的属性来触发 getter，将属性与 DOM 元素建立关联。
> 3. **Watcher 监听**：每个数据都有一个对应的 Watcher 实例，当数据发生变化时，会触发对应 Watcher 的 update 方法，进而更新对应的 DOM 元素。
> 4. **响应式原理**：当触发 setter 时，会通知相关的 Watcher，从而更新视图。这样就实现了数据到视图的单向绑定。
>
> Vue 3 的双向绑定原理：
>
> Vue 3 中采用了 Proxy 来替代 Object.defineProperty 来实现数据劫持，Proxy 相比于 Object.defineProperty 具有更好的性能和更丰富的功能。 在 Vue 3 中，当数据发生变化时，会通过触发 setter 来通知对应的依赖进行更新。这种方式相比 Vue 2 更加高效和灵活。

#### 路由的模式

> 1. **Hash 模式（Hash History）**：在 URL 中使用 # 符号来作为路由地址的标识。例如，`http://example.com/#/about`。在这种模式下，当 URL 发生变化时，页面不会重新加载，而是通过监听 `hashchange` 事件来改变页面内容。优点是兼容性好，可以在不支持 History API 的浏览器中使用。缺点是 URL 中会出现 `#` 符号，看起来不太美观，并且对 SEO 不友好。
> 2. **History 模式（HTML5 History API）**：利用 HTML5 History API 中的 `pushState` 和 `replaceState` 方法来管理路由。在这种模式下，URL 是真实的 URL，没有 `#` 符号。例如，`http://example.com/about`。当 URL 发生变化时，页面不会重新加载，而是通过调用 History API 来改变页面内容。优点是 URL 更美观，并且对 SEO 更友好。缺点是需要服务器端的配合，以处理在客户端路由变化时的请求。
> 3. **Memory 模式**：仅在内存中保存路由状态，不会改变 URL。这种模式通常用于不需要在浏览器地址栏中显示路由信息的场景，如在一个单页面应用中的弹窗或抽屉组件中切换内容。

#### 介绍一下vuex和pinia

> ### Vuex：
>
> 1. **核心概念**：Vuex 包含了一些核心概念，如 state（状态）、mutations（同步修改状态的方法）、actions（异步修改状态的方法）、getters（对 state 进行计算属性）、modules（模块化组织 state、mutations、actions、getters）等。
> 2. **严格模式**：Vuex 提供了严格模式，可以帮助开发者更好地调试应用程序中的状态变化。
> 3. **插件**：Vuex 支持插件机制，可以扩展其功能，如持久化存储等。
> 4. **使用场景**：适用于中大型 Vue.js 项目，需要管理较为复杂的状态逻辑和数据流。
>
> ### Pinia：
>
> 1. **Vue 3 的状态管理库**：Pinia 是为 Vue 3 开发的状态管理库，在设计上更符合 Vue 3 的响应式系统。
> 2. **Composition API 风格**：Pinia 的 API 设计借鉴了 Vue 3 的 Composition API，使用起来更加灵活和直观。
> 3. **支持 TypeScript**：Pinia 对 TypeScript 的支持更好，可以提供更好的类型推断和安全性。
> 4. **模块化组织**：Pinia 也支持模块化组织状态，可以更好地管理状态逻辑。
> 5. **轻量级**：相较于 Vuex，Pinia 更轻量级，对于小型项目或简单的状态管理需求更加适合。

#### Keep-alive

> 

## <font style="font-size:24px;font-weight:600;">React问题</font>

#### vue和react区别

> Vue.js 更注重简单和直观，提供了双向数据绑定和模板语法，适合快速开发和小型项目；
>
> 而 React 更注重灵活性和性能，使用 JSX 和单向数据流，适合大型应用和复杂场景。
>
> 选择使用哪个框架取决于项目需求、团队技术栈和个人偏好。

1. **设计理念**：
   - Vue.js 的设计目标是尽可能简单，容易上手，同时也具有灵活性和高可定制性。
   - React 的设计理念是通过组件化构建用户界面，引入虚拟 DOM 提高性能。
2. **语法**：
   - Vue.js 使用类似于 HTML 的模板语法，可以更直观地描述界面的结构和逻辑。
   - React 使用 JSX（JavaScript XML），将 HTML 和 JavaScript 结合在一起，提供了更灵活和强大的组件化能力。
3. **数据绑定**：
   - Vue.js 使用双向数据绑定，可以在模板中直接使用变量，并且当数据变化时，视图会自动更新。
   - React 使用单向数据流，通过 props 和 state 来管理数据流动，数据的变化会引起组件的重新渲染。
4. **组件化**：
   - Vue.js 和 React 都支持组件化，但在使用方式上有一些不同。Vue.js 的组件化更加直观，可以通过一个文件包含模板、样式和逻辑；React 则将模板、样式和逻辑分离开，更加灵活。
5. **生态系统**：
   - React 生态系统更加庞大，有丰富的第三方库和工具支持，如 Redux、React Router、Material-UI 等。
   - Vue.js 生态系统也在不断发展壮大，有一些类似于 Vuex、Vue Router、Element-UI 等的库和工具。
6. **学习曲线**：
   - Vue.js 的学习曲线相对较低，容易上手，适合初学者。
   - React 的学习曲线较陡，需要一定的 JavaScript 和前端知识基础。

#### 说说对 React 的理解？有哪些特性？

> 用于构建用户界面的js库，
>
> 采用组件化开发模式使代码复用性提高，减少重复代码量，组件组合使用使构建复杂页面更简单，
>
> 虚拟dom减少频繁操作真实DOM的开发成本，并通过diff算法实现页面局部更新，提高渲染性能。
>
> 声明式编程使开发时更关注业务逻辑实现，提高开发效率

### <font style="font-size:18px;font-weight:600;">虚拟Dom是什么？</font>

> 虚拟DOM是相对于浏览器所渲染出来的真实DOM而言的，虚拟DOM（Virtual DOM）就是普通的js对象，用来描述真实dom结构
>
> 虚拟DOM的性能优势在于对DOM的子孙节点进行增删改操作。

### <font style="font-size:18px;font-weight:600;">虚拟Dom比 真实Dom更快吗？</font>

任何情况下，直接操作真实DOM，性能肯定高于使用虚拟DOM渲染这个完整的DOM，因为虚拟DOM还要进行转化为真实DOM的操作。

上面说的虚拟DOM性能更优，指的是在某些场景下的某些操作。

虚拟DOM的性能优势在于对DOM的子孙节点进行增删改操作。

它通过批量更新 DOM、减少重绘和回流等方式提高了性能，同时也增加了一定的复杂性。

虚拟 DOM 在需要频繁更新的场景下可以发挥更大的优势，但在简单的应用中可能性能提升并不明显，需要根据具体情况进行权衡。

- 虚拟DOM不一定会比操作原生DOM更快。
- 虚拟DOM的优势在于节点进行改动的时候尽量减少开销
- 框架的本质是提升开发效率，让我们的注意力更集中于数据

### <font style="font-size:18px;font-weight:600;">虚拟Dom中的 $typeof属性的作用是什么？</font>

> 用于标识 React 元素的类型。它是一个 Symbol 值，用于确保 React 元素不会被错误地当作普通对象来处理。

### <font style="font-size:18px;font-weight:600;">React在渲染 真实Dom时做了哪些性能优化？</font>

> React 在渲染真实 DOM 时通过虚拟 DOM、批量更新、DOM Diff 算法等方式进行性能优化。这些优化措施可以减少不必要的 DOM 操作，提高渲染效率和用户体验。

### <font style="font-size:18px;font-weight:600;">React生命周期有哪些？</font>

> 生命周期是一个程序从运行到结束的过程
>
> React 生命周期包括render（初始化） Mounting（挂载）、Updating（更新）、Unmounting（卸载）。
>
> Mounting 阶段包括 constructor、getDerivedStateFromProps、render 和 componentDidMount；
>
> Updating 阶段包括 static getDerivedStateFromProps、shouldComponentUpdate、render、getSnapshotBeforeUpdate 和 componentDidUpdate；
>
> Unmounting 阶段包括 componentWillUnmount。

#### 错误处理

当渲染过程，生命周期或子组件的构造函数中抛出错误时，会调用如下方法：

- static getDerivedStateFromProps()
- componentDidCatch()

### <font style="font-size:18px;font-weight:600;">setState是同步的还是异步的？</font>

​    [setState](https://so.csdn.net/so/search?q=setState&spm=1001.2101.3001.7020)在合成事件和生命周期函数中是异步的，在原生事件和定时器中都是同步的。

​    setState本身不分同步或者异步，而是取决于是否处于batch update中。组件中的所有函数在执行时临时设置一个变量isBatchingUpdates = true，当遇到setState时，如果isBatchingUpdates是true，那么setState就是异步的，如果是false，那么setState就是同步的。那么什么时候isBatchingUpdates会被设置成false呢？

​    1当函数运行结束时isBatchingUpdates = false

​    2 当函数遇到setTimeout、setInterval时isBatchingUpdates = false

​    3 当dom添加自定义事件时isBatchingUpdates = false

### <font style="font-size:18px;font-weight:600;">为什么有时连续多次 setState只有一次生效？</font>

> 这种情况通常是由于 React 的批量更新机制所导致的。
>
> React 会将连续多次的 `setState` 调用合并成一个更新，只触发一次重新渲染，以提高性能。
>
> 如果需要确保每次 `setState` 都触发更新，可以使用函数形式的 `setState`，这样可以确保每次都是基于最新的状态进行更新，不会被批量处理掉。

### <font style="font-size:18px;font-weight:600;">React如何实现自己的事件机制？</font>

> 可以借助 React 的 Context 和自定义 Hook。
>
> 通过创建一个 Context 来存储事件处理函数和触发事件的方法，然后使用自定义 Hook 来注册事件处理函数和触发事件。这样就可以在组件中方便地使用自定义的事件机制。
>
> React 使用合成事件来处理 DOM 事件，这种事件系统提供了一种更高效、更一致的方式来处理事件。
>
> 可以通过在组件中维护一个事件监听器列表
>
> 在`componentDidMount` 和 `componentWillUnmount` 生命周期方法中添加和移除自定义事件监听器。
>
> 自定义事件的使用场景包括但不限于跨组件通信、解耦组件逻辑、实现特定功能等。

### <font style="font-size:18px;font-weight:600;">为何 React事件要自己绑定 this？</font>

`this`指向的是函数运行时所在的环境对象，普通函数中`this`指向是调用函数的对象，而且是离谁近指向谁。

箭头函数的`this`指向取决于外层（上一层）作用域中的`this`，在定义时就已经确定好了`this`的指向, 不会根据谁调用它而改变。

对于普通函数而言，其`this`的指向还与是否为严格模式有关：在顶级作用域中，非严格模式下普通函数中的`this`指向`window`对象；而严格模式下普通函数中的`this`则为`undefined`。

> 函数中的 this 指向函数的调用者。obj.fn() 则 fn 中的 this 指向 obj；如果 cb = obj.fn()，再调用 cb() 则 this 指向 window。中间变量 cb 的调用者为 window。
>
> 然而在 react 中，传递的事件参数不是一个字符串，而是一个实实在在的函数。react 在事件发生时调用 onClick，由于onClick只是中间变量，所以处理函数中的this指向会丢失，为了解决这个问题，我们需要在实例化对象的时候，在构造函数中绑定 this，使得无论事件处理函数如何传递，它的 this 的指向都是固定的，固定指向我们所实例化的对象。

### <font style="font-size:18px;font-weight:600;">原生事件和 React事件的区别？</font>

`react事件是绑定在document上的`；

`原生事件是绑定在DOM上的`；

react的事件对象是合成的，不是原生的；

`dom上的事件要优先于document上的事件执行`。事件的执行顺序为原生事件先执行，合成事件后执行，合成事件会[冒泡](https://so.csdn.net/so/search?q=冒泡&spm=1001.2101.3001.7020)绑定到 document 上，所以尽量避免原生事件与合成事件混用，如果原生事件阻止冒泡，可能会导致合成事件不执行，因为需要冒泡到 document 上合成事件才会执行。

**react在底层，主要对合成事件做了2件事：**

1. 事件委派：把所有事件绑定到结构的最外层，使用统一的事件监听，这个事件监听器上维持了一个映射来保存所有组件内部事件监听和处理函数。
2. 自动绑定： React组件中，每个方法的上下文都会指向该组件的实例，即自动绑定this为当前组件。

**优点：**

1. 几乎所有的事件都代理（delegate）到document，达到性能优化的目的
2. 兼容所有浏览器，更好的跨平台。
3. 方便 react 统一管理和事务机制。

### <font style="font-size:18px;font-weight:600;">React的合成事件是什么？</font>

React 的合成事件是指 React 在原生 DOM 事件的基础上封装的一层事件系统，通过冒泡事件冒泡到顶层元素，再统一分发处理

使用合成事件可以使得事件处理在跨浏览器和性能方面更加稳定和高效。合成事件提供了跨浏览器兼容性、事件池管理、性能优化、事件代理等优点，同时也减少了手动绑定事件的工作量。

### <font style="font-size:18px;font-weight:600;">React和原生事件的执行顺序是什么？可以混用吗？</font>

`dom上的事件要优先于document上的事件执行`。

事件的执行顺序为原生事件先执行，合成事件后执行，合成事件会[冒泡](https://so.csdn.net/so/search?q=冒泡&spm=1001.2101.3001.7020)绑定到 document 上，

所以尽量避免原生事件与合成事件混用，如果原生事件阻止冒泡，可能会导致合成事件不执行，

因为需要冒泡到 document 上合成事件才会执行。

### <font style="font-size:18px;font-weight:600;">React组件的渲染流程是什么？</font>

React 组件的渲染流程可以分为初始化阶段、挂载阶段、更新阶段和卸载阶段。

在挂载阶段，React 首先调用组件的构造函数初始化组件，然后调用 `render` 方法生成虚拟 DOM，并将虚拟 DOM 转化为真实 DOM 插入到页面中。

在更新阶段，当组件的状态或属性发生变化时，React 会重新生成虚拟 DOM，并使用 Diff 算法找出需要更新的部分，最后将更新的部分反映到页面中。

在卸载阶段，React 调用 `componentWillUnmount` 生命周期方法进行清理工作，然后将组件从 DOM 中移除。

整个过程中，React 通过虚拟 DOM 和 Diff 算法来最小化 DOM 操作，提高性能。

### <font style="font-size:18px;font-weight:600;">为什么代码中一定要引入 React？</font>

本质上来说JSX是`React.createElement(component, props, ...children)`方法的语法糖。

所以我们如果使用了JSX，我们其实就是在使用React，所以我们就需要引入React

### <font style="font-size:18px;font-weight:600;">为什么 React组件首字母必须大写？</font>

为了区分普通的 HTML 元素和 React 组件。在 JSX 中，如果组件的首字母是小写，那么 React 将会把它视为一个 HTML 原生元素

### <font style="font-size:18px;font-weight:600;">什么是高阶组件？如何实现？</font>

> 高阶组件是一个函数，接受一个组件作为参数，并返回一个新的组件。
>
> 通过高阶组件，我们可以实现组件逻辑的复用，例如添加新的行为、属性或其他功能。
>
> 实现一个高阶组件的步骤包括定义一个函数，接受组件作为参数，并在函数内部定义一个新的组件，最后返回这个新组件。

### <font style="font-size:18px;font-weight:600;">HOC在业务场景中有哪些实际应用场景？</font>

> 可以用于代码复用，将组件逻辑封装在一个函数中，在多个组件之间共享和复用逻辑代码
>
> 还可以用于状态管理，通过 HOC 在组件中管理状态，例如使用 Redux 或者 Context API 管理全局状态，并将状态传递给组件；
>
> 还可以用于权限控制、数据获取、性能优化、日志记录、错误边界等方面。
>
> 总的来说，HOC 可以帮助开发者简化代码、提高代码复用性、实现高级功能和优化性能。

### <font style="font-size:18px;font-weight:600;">高阶组件( HOC)和 Mixin的异同点是什么？</font>

> 高阶组件（HOC）和 Mixin 都是用于在 React 组件中实现代码复用的技术
>
> HOC 是一个函数，接受一个组件作为参数，并返回一个新的组件，通过包裹原始组件来实现功能的复用；
>
> 而 Mixin 则是一种在组件中混入额外功能的方式，直接影响组件本身。
>
> 在 React 中，由于 Mixin 的一些限制和不推荐使用，通常更推荐使用 HOC 来实现组件间的代码复用和功能增强。

### <font style="font-size:18px;font-weight:600;">Hook有哪些优势？</font>

> 它可以更容易地复用逻辑、理解和测试，减少代码量，更好地优化性能，实现更灵活的组件结构，同时减少对生命周期的依赖。
>
> 1. **更容易复用逻辑**：Hooks 允许您将组件逻辑封装为可重用的函数，使得在组件之间共享逻辑变得更加容易。
> 2. **更容易理解**：相比于类组件中的生命周期方法，Hooks 更直观、更易于理解，可以更快速地掌握和使用。
> 3. **减少代码量**：Hooks 可以减少组件中的样板代码量，使代码更加简洁。
> 4. **更容易测试**：由于逻辑可以单独封装在 Hooks 中，因此更容易编写和运行测试。
> 5. **更好的性能优化**：Hooks 可以帮助 React 更有效地跟踪组件状态的变化，从而更好地进行性能优化。
> 6. **更灵活的组件结构**：Hooks 可以在不编写类组件的情况下使用状态和其他 React 功能，使得组件结构更加灵活。
> 7. **减少生命周期的使用**：Hooks 可以减少对生命周期方法的依赖，使组件更加简洁和易于维护。

## <font style="font-size:24px;font-weight:600;">浏览器问题</font>

### <font style="font-size:24px;font-weight:600;">构建工具问题</font>

### <font style="font-size:24px;font-weight:600;">Node问题</font>