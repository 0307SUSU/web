# 微信原生小程序开发

### 入口构成

> app.js
>
> app.json    小程序的全局配置
>
> ```json
> // 全局配置的所有配置项及各个配置项作用
> {
>     "entryPagePath":'',// 小程序默认启动首页    非必填
>     "pages":[   //页面路径配置 必填
>     "pages/index/index",
>     "pages/logs/logs"
>   ],
>   "window":{   //  全局页面顶部样式配置
>     "backgroundTextStyle":"light",
>     "navigationBarBackgroundColor": "#fff",
>     "navigationBarTitleText": "Weixin", //  配置顶部标题文本
>     "navigationBarTextStyle":"black"
>   }
>     
> }
> ```
>
> 
>
> app.wxss
>
> 工具配置 project.config.json
>
> 

### 页面构成

> 每个页面都有四个基本的文件
>
> page.json
>
> page.wxml
>
> page.wxss
>
> page.js

### 生命周期

> 初始化、挂载、销毁
>
> **onPageNotFound(options)**、**onUnhandledRejection(reason)**、
>
> | 参数属性 | 类型     | 描述                                                         |
> | :------- | :------- | :----------------------------------------------------------- |
> | onLaunch | Function | 当小程序初始化完成时，会触发 onLaunch（全局只触发一次）      |
> | onShow   | Function | 当小程序启动，或从后台进入前台显示，会触发 onShow            |
> | onHide   | Function | 当小程序从前台进入后台，会触发 onHide                        |
> | onError  | Function | 当小程序发生脚本错误，或者 API 调用失败时，会触发 onError 并带上错误信息 |
> | 其他字段 | 任意     | 可以添加任意的函数或数据到 Object 参数中，在App实例回调用 this 可以访问 |
>
> 页面的生命周期
>
> **onLoad(options)**、**onShow()**、**onReady()**、**onHide()**、**onUnload()**

### 路由

> 1. **navigateTo(url)**：保留当前页面，跳转到应用内的某个页面，使用wx.navigateBack可以返回到原页面。最多打开5个页面。
> 2. **redirectTo(url)**：关闭当前页面，跳转到应用内的某个页面。不允许跳转到tabBar页面。
> 3. **reLaunch(url)**：关闭所有页面，打开到应用内的某个页面。
> 4. **navigateBack(delta)**：关闭当前页面，返回上一页面或多级页面。
> 5. **switchTab**

### 全局配置

> 整个应用的配置，在app.json文件中，配置项可参考官方文档https://developers.weixin.qq.com/miniprogram/dev/reference/configuration/app.html

### 页面配置

> 单个页面的配置，每个页面都有一个.json文件，页面的配置就写在这个文件中，配置项参考https://developers.weixin.qq.com/miniprogram/dev/reference/configuration/page.html

### API

> 网络
>
> 请求：wx.request
>
> 下载：wx.downloadFile
>
> 上传：wx.uploadFile
>
> 支付
>
> wx.requestVirtualPayment(Object object)
>
> wx.requestPluginPayment(Object object)
>
> wx.requestPayment(Object object)
>
> wx.requestMerchantTransfer(Object object)
>
> wx.requestCommonPayment(Object object)
>
> 数据缓存
>
> wx.setStorage(Object object)
>
> wx.removeStorage(Object object)
>
> wx.getStorage(Object object)
>
> wx.getStorageInfo(Object object)
>
> wx.clearStorage(Object object)
>
> wx.batchSetStorage(Object object)
>
> wx.batchGetStorage(Object object)
>
> 图片
>
> 视频
>
> 地图
>
> 音频
>
> 背景音频
>
> 实时音视频
>
> 录音
>
> 相机
>
> 
>
> 

### 设置公共组件

> 1. 首先在项目根目录中创建components文件夹
>
> 2. 在components文件夹中创建组件文件夹，例如swiper（轮播）
>
> 3. 新建组件文件
>
>    .wxml  设置组件结构
>
>    ```html
>    <swiper class="swiper-container" indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
>      <block wx:for="{{images}}" wx:key="url">
>        <swiper-item style="width: 100%;"> 
>          <image class="slide-image" src="{{item.url}}" mode="aspectFill"></image>
>        </swiper-item>
>      </block>
>    </swiper>
>    ```
>
>    .wxss    设置组件样式
>
>    ```css
>    .swiper-container {
>      width: 100%;
>      height: 200px;
>    }
>    
>    .slide-image {
>      width: 100%;
>      height: 100%;
>    }
>    ```
>
>    .js    设置组件逻辑
>
>    ```js
>    // 公共组件方法内配置要暴露的属性
>    Component({
>      properties: {
>        images: {
>          type: Array,
>          value: []
>        },
>        indicatorDots: {
>          type: Boolean,
>          value: true
>        },
>        autoplay: {
>          type: Boolean,
>          value: true
>        },
>        interval: {
>          type: Number,
>          value: 5000
>        },
>        duration: {
>          type: Number,
>          value: 500
>        }
>      }
>    });
>    ```
>
>    .json    配置json,将component设置为true
>
>    ```json
>    {
>      "component": true,
>      "usingComponents": {}
>    }
>    ```
>
> 4. 在要使用的组件中引入并使用该组件
>
>    引入
>
>    .json文件的usingComponents中引入
>
>    ```json
>    {
>        "usingComponents": {
>        "swiper-view":"/components/swiper/swiperView"
>      },
>    }
>    ```
>
>    使用
>
>    .wxml文件中使用
>
>    ```html
>    <!--在组建的开始标签中设置组件暴露出来的属性-->
>    <swiper-view images="{{images}} indicatorDots="{{true}}" autoplay="{{true}}" interval="{{5000}}" duration="{{500}}" "></swiper-view>
>    ```
>
> 5. 全局引入并在任何组件下使用
>
>    在app.json文件中引入
>
>    ```json
>    {
>        "usingComponents": {
>        "swiper-view":"/components/swiper/swiperView"
>      },
>    }
>    ```
>
>    

### 接口调用的封装

1. 